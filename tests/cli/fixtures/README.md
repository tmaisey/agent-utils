# Fixtures

Synthetic repos consumed by `tests/cli/*.bats`. Keep each one minimal and self-documenting.

| Fixture | Used by | Contents |
|---------|---------|----------|
| _(none yet)_ | | |

When adding a fixture: include a one-line `README.md` inside it stating what's planted and which test reads it.
